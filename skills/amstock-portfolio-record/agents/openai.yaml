interface:
  display_name: "AMStock Portfolio Record"
  short_description: "Record stock transactions and calculate portfolio returns in a standalone JSON ledger."
  default_prompt: "Use this skill only when the user wants the standalone JSON ledger; otherwise prefer `amstock portfolio` for the configured AMStock database ledger."
