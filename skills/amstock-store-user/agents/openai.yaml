interface:
  display_name: "AMStock Store User"
  short_description: "Record, delete, and calculate local portfolio transactions."
  default_prompt: "Use this skill with `amstock portfolio` to record AMStock local store buy/sell/import transactions, delete transaction records, list trade history, view holdings, and calculate realized or unrealized returns."
